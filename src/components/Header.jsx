import React from 'react';
import { styled } from 'styled-components';

const Header = () => {
  return <StDiv>Header</StDiv>;
};

export default Header;

const StDiv = styled.div`
  height: 75px;
  background-color: var(--color_navy);
`;
